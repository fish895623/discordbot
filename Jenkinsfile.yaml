pipeline:
  agent: any
    stages:
    - stage: "Parrallel Test"
      parallel:
        - stage: "build-test-1"
          steps:
            - sh "echo 1"
        - stage: "build-test-2"
          steps:
            - sh "echo 1"
        - stage: "build-test-3"
          steps:
            - sh "echo 1"
    - stage: "build Docker"
      steps:
        - sh "docker-compose build"
